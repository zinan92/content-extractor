---
phase: 08-llm-analysis
plan: 02
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - src/content_extractor/extract.py
  - src/content_extractor/output.py
  - tests/test_extract.py
  - tests/test_output.py
autonomous: true
requirements:
  - ANLYS-05
  - ANLYS-06
must_haves:
  truths:
    - "Running extract_content() on a ContentItem now produces a filled analysis.json with real topics, viewpoints, sentiment, and takeaways"
    - "structured_text.md contains actual Summary, Key Takeaways, and Analysis sections derived from LLM output (not placeholder text)"
    - "When analysis fails (LLM error), extraction still succeeds with a placeholder analysis and a warning logged"
  artifacts:
    - path: "src/content_extractor/extract.py"
      provides: "Pipeline wiring: calls analyze_content() after adapter extraction"
      contains: "analyze_content"
    - path: "src/content_extractor/output.py"
      provides: "Updated structured_text renderer and output writer accepting real AnalysisResult"
      contains: "_render_structured_text"
  key_links:
    - from: "src/content_extractor/extract.py"
      to: "src/content_extractor/analysis.py"
      via: "analyze_content() call"
      pattern: "analyze_content"
    - from: "src/content_extractor/output.py"
      to: "src/content_extractor/models.py"
      via: "AnalysisResult used in rendering"
      pattern: "AnalysisResult"
    - from: "src/content_extractor/extract.py"
      to: "src/content_extractor/output.py"
      via: "passes AnalysisResult to write_extraction_output"
      pattern: "write_extraction_output.*analysis"
---

<objective>
Wire the analysis module into the extraction pipeline and update the output writer to produce real analysis.json and structured_text.md content.

Purpose: Connect the analysis engine (Plan 01) to the existing extraction flow so every extracted ContentItem gets analyzed and produces complete output files with real content instead of placeholders.

Output: Updated extract.py and output.py with analysis integration + updated tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-llm-analysis/08-RESEARCH.md
@.planning/phases/08-llm-analysis/08-01-SUMMARY.md
@src/content_extractor/extract.py
@src/content_extractor/output.py
@src/content_extractor/models.py
@src/content_extractor/config.py
@tests/test_extract.py
@tests/test_output.py

<interfaces>
<!-- From Plan 01 output -->
From src/content_extractor/analysis.py (created in 08-01):
```python
class AnalysisError(Exception): ...

def analyze_content(
    raw_text: str,
    *,
    content_id: str,
    content_type: str,
    config: ExtractorConfig | None = None,
) -> AnalysisResult: ...
```

From src/content_extractor/extract.py (current):
```python
def extract_content(content_dir: Path, config=None) -> ExtractionResult | None:
    # load -> route -> adapt -> write output
    # Currently: adapter.extract() -> write_extraction_output(content_dir, result, item)

def extract_batch(parent_dir: Path, config=None) -> BatchResult:
```

From src/content_extractor/output.py (current):
```python
def write_extraction_output(content_dir, result, content_item, *, force=False) -> bool:
    # Currently creates placeholder AnalysisResult internally
    # _render_structured_text() outputs "*Populated by analysis phase.*" placeholders
```

From src/content_extractor/models.py:
```python
class AnalysisResult(BaseModel):
    content_id: str
    content_type: str
    topics: tuple[str, ...] = ()
    viewpoints: tuple[str, ...] = ()
    sentiment: SentimentResult | None = None
    takeaways: tuple[str, ...] = ()
    schema_version: str = "1.0"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire analysis into extract.py pipeline</name>
  <files>src/content_extractor/extract.py, tests/test_extract.py</files>
  <behavior>
    - Test 1: extract_content() calls analyze_content() after adapter.extract() and passes the result to write_extraction_output()
    - Test 2: When analyze_content() raises AnalysisError, extract_content() catches it, logs a warning, and continues with a placeholder AnalysisResult (extraction does NOT fail)
    - Test 3: extract_batch() still isolates per-item errors -- analysis failures in one item do not affect others
  </behavior>
  <action>
    Modify `src/content_extractor/extract.py`:

    1. Import `analyze_content` and `AnalysisError` from `content_extractor.analysis`.
    2. Import `logging` and create a module logger.
    3. In `extract_content()`, after `result = adapter.extract(content_dir, config)`:
       - Call `analyze_content(raw_text=result.raw_text, content_id=result.content_id, content_type=result.content_type, config=config)`.
       - Wrap the call in try/except `AnalysisError` -- on failure, log warning and create a fallback `AnalysisResult(content_id=result.content_id, content_type=result.content_type)`.
       - Store the `AnalysisResult` in a local variable `analysis`.
    4. Update the `write_extraction_output()` call to pass `analysis=analysis` as a new keyword argument.
    5. `extract_batch()` needs no changes -- it already isolates per-item errors via the try/except in the loop.

    Update `tests/test_extract.py`:
    - Add tests for the 3 behaviors above.
    - Mock `analyze_content` to return a canned AnalysisResult.
    - Mock `analyze_content` to raise AnalysisError and verify extraction still succeeds.
    - Ensure existing tests still pass (they should with the mock).
  </action>
  <verify>
    <automated>cd /Users/wendy/work/content-co/content-extractor && python -m pytest tests/test_extract.py -x -v</automated>
  </verify>
  <done>
    - extract_content() calls analyze_content() and passes result downstream
    - Analysis failures are caught gracefully with warning log, extraction continues
    - All existing + new extract tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update output writer for real analysis content</name>
  <files>src/content_extractor/output.py, tests/test_output.py</files>
  <behavior>
    - Test 1: write_extraction_output() with a filled AnalysisResult writes analysis.json with real topics, viewpoints, sentiment, takeaways
    - Test 2: _render_structured_text() with a filled AnalysisResult renders actual Summary (first takeaway or topic sentence), Key Takeaways (bulleted list), and Analysis (topics + viewpoints + sentiment)
    - Test 3: _render_structured_text() with empty AnalysisResult still renders valid markdown (no crash, sections say "No analysis available")
    - Test 4: write_extraction_output() signature accepts optional analysis parameter; when None, writes placeholder (backward compatible)
  </behavior>
  <action>
    Modify `src/content_extractor/output.py`:

    1. Update `write_extraction_output()` signature to accept `analysis: AnalysisResult | None = None`.
       - When `analysis` is provided, use it for analysis.json instead of the placeholder.
       - When `analysis` is None, fall back to current placeholder behavior (backward compatible for callers that don't pass analysis yet).

    2. Update `_render_structured_text()` to accept an optional `analysis: AnalysisResult | None = None` parameter:
       - **Summary section:** If analysis has topics, render a 1-2 sentence summary like "This content covers: {', '.join(topics)}." If no topics, keep "No analysis available."
       - **Key Takeaways section:** If analysis has takeaways, render as a bulleted markdown list (`- {takeaway}`). If empty, "No takeaways identified."
       - **Analysis section:** Render structured subsections:
         - **Topics:** comma-separated list
         - **Viewpoints:** bulleted list
         - **Sentiment:** "Overall: {sentiment.overall} (confidence: {sentiment.confidence})" or "Not assessed" if None
       - If analysis is None or all fields empty, render "No analysis available."

    3. Update the `write_extraction_output()` call to `_render_structured_text()` to pass the analysis parameter.

    Update `tests/test_output.py`:
    - Add tests for the 4 behaviors above.
    - Verify analysis.json content is correct when AnalysisResult is provided.
    - Verify structured_text.md has real content, not placeholders.
    - Verify backward compatibility when analysis=None.
    - Ensure all existing output tests still pass.
  </action>
  <verify>
    <automated>cd /Users/wendy/work/content-co/content-extractor && python -m pytest tests/test_output.py -x -v</automated>
  </verify>
  <done>
    - analysis.json contains real analysis data when AnalysisResult is provided
    - structured_text.md renders Summary, Key Takeaways, Analysis with actual content per D-04
    - Backward compatible: analysis=None produces placeholder behavior
    - All existing + new output tests pass
  </done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_extract.py tests/test_output.py tests/test_analysis.py -x -v` -- all tests pass
- `python -m pytest tests/ -x --tb=short` -- full test suite passes (no regressions)
- `ruff check src/content_extractor/extract.py src/content_extractor/output.py` -- no lint errors
</verification>

<success_criteria>
- extract_content() produces real analysis.json with topics, viewpoints, sentiment, takeaways (ANLYS-05)
- structured_text.md has actual Summary, Key Takeaways, Analysis sections (ANLYS-06, D-04)
- Analysis failures do not break extraction -- graceful degradation to placeholder
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/08-llm-analysis/08-02-SUMMARY.md`
</output>
