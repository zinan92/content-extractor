---
phase: 01-foundation-data-models
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/content_extractor/adapters/__init__.py
  - src/content_extractor/adapters/base.py
  - src/content_extractor/adapters/video.py
  - src/content_extractor/adapters/image.py
  - src/content_extractor/adapters/article.py
  - src/content_extractor/adapters/gallery.py
  - src/content_extractor/router.py
  - src/content_extractor/output.py
  - tests/test_router.py
  - tests/test_output.py
autonomous: true
requirements:
  - FOUND-02
  - FOUND-04
  - FOUND-05
  - QUAL-03

must_haves:
  truths:
    - "Content routes to the correct adapter stub based on content_type (video/image/article/gallery)"
    - "Unknown content_type raises UnsupportedContentTypeError with list of supported types"
    - "Output files (transcript.json, analysis.json, structured_text.md) are written atomically"
    - "Re-running extraction on already-processed content skips it; --force re-processes it"
    - "Partial output is never left behind on failure (atomic writes)"
  artifacts:
    - path: "src/content_extractor/adapters/base.py"
      provides: "Extractor Protocol definition"
      exports: ["Extractor"]
      min_lines: 10
    - path: "src/content_extractor/router.py"
      provides: "Content type routing registry"
      exports: ["register", "get_extractor", "UnsupportedContentTypeError"]
    - path: "src/content_extractor/output.py"
      provides: "Atomic file writer with idempotency"
      exports: ["write_json_atomic", "write_text_atomic", "is_extracted", "mark_complete", "clear_marker", "write_extraction_output"]
      min_lines: 40
  key_links:
    - from: "src/content_extractor/router.py"
      to: "src/content_extractor/adapters/base.py"
      via: "imports Extractor Protocol"
      pattern: "from content_extractor\\.adapters\\.base import Extractor"
    - from: "src/content_extractor/output.py"
      to: "src/content_extractor/models.py"
      via: "imports ExtractionResult, Transcript, AnalysisResult"
      pattern: "from content_extractor\\.models import"
---

<objective>
Build the adapter registry (content_type routing) and atomic output writer with idempotency guard.

Purpose: The router dispatches content to the right adapter, and the output writer safely persists extraction results. Together they form the processing pipeline skeleton that Phase 2+ adapters plug into.

Output: Adapter Protocol, 4 stub adapters, router with registry, atomic output writer with .extraction_complete marker, and passing tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-data-models/01-CONTEXT.md
@.planning/phases/01-foundation-data-models/01-RESEARCH.md
@.planning/phases/01-foundation-data-models/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 -- models that this plan depends on -->

From src/content_extractor/models.py:
```python
class ContentItem(BaseModel):
    model_config = ConfigDict(frozen=True, extra="ignore")
    platform: str
    content_id: str
    content_type: str  # "video" | "image" | "article" | "gallery"
    # ... 18 fields total

class ExtractionResult(BaseModel):
    model_config = ConfigDict(frozen=True)
    content_id: str
    content_type: str
    raw_text: str
    transcript: Transcript | None = None
    media_descriptions: tuple[MediaDescription, ...] = ()
    quality: QualityMetadata = QualityMetadata()
    platform_metadata: dict[str, int | str] = {}
    schema_version: str = "1.0"

class Transcript(BaseModel):
    # content_id, content_type, language, segments, full_text, schema_version

class AnalysisResult(BaseModel):
    # content_id, content_type, topics, viewpoints, sentiment, takeaways, schema_version
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adapter Protocol, stub adapters, and router with registry</name>
  <files>
    src/content_extractor/adapters/__init__.py,
    src/content_extractor/adapters/base.py,
    src/content_extractor/adapters/video.py,
    src/content_extractor/adapters/image.py,
    src/content_extractor/adapters/article.py,
    src/content_extractor/adapters/gallery.py,
    src/content_extractor/router.py,
    tests/test_router.py
  </files>
  <read_first>
    /Users/wendy/work/content-co/content-extractor/src/content_extractor/models.py,
    /Users/wendy/work/content-co/content-extractor/.planning/phases/01-foundation-data-models/01-RESEARCH.md
  </read_first>
  <behavior>
    - get_extractor("video") returns VideoExtractor instance
    - get_extractor("image") returns ImageExtractor instance
    - get_extractor("article") returns ArticleExtractor instance
    - get_extractor("gallery") returns GalleryExtractor instance
    - get_extractor("podcast") raises UnsupportedContentTypeError with "Supported: ['article', 'gallery', 'image', 'video']"
    - All stub adapters raise NotImplementedError when extract() is called
    - All stub adapters satisfy the Extractor Protocol (isinstance check passes)
  </behavior>
  <action>
    1. Create `src/content_extractor/adapters/__init__.py`: empty, just docstring.

    2. Create `src/content_extractor/adapters/base.py` per 01-RESEARCH.md Pattern 3:
       - `@runtime_checkable class Extractor(Protocol)`:
         - `content_type: str`
         - `def extract(self, content_dir: Path, config: "ExtractorConfig") -> ExtractionResult: ...`
       - Import Path from pathlib, Protocol and runtime_checkable from typing
       - Forward reference "ExtractorConfig" as string to avoid circular import

    3. Create 4 stub adapters (video.py, image.py, article.py, gallery.py), each:
       - Class with `content_type: str` class variable set to the appropriate type string
       - `extract()` method that raises `NotImplementedError(f"{self.content_type} extraction not yet implemented")`
       - Example for video.py: `class VideoExtractor: content_type = "video"`

    4. Create `src/content_extractor/router.py`:
       - `class UnsupportedContentTypeError(Exception): pass`
       - Module-level `_REGISTRY: dict[str, type]` initialized empty
       - `register(content_type: str, extractor_cls: type) -> None`: adds to _REGISTRY
       - `get_extractor(content_type: str) -> Extractor`: looks up in _REGISTRY, raises UnsupportedContentTypeError if not found with message showing sorted supported types
       - At module bottom, auto-register all 4 adapters:
         ```python
         register("video", VideoExtractor)
         register("image", ImageExtractor)
         register("article", ArticleExtractor)
         register("gallery", GalleryExtractor)
         ```

    5. Create `tests/test_router.py`:
       - test_get_video_extractor: get_extractor("video") returns VideoExtractor instance
       - test_get_image_extractor: get_extractor("image") returns ImageExtractor instance
       - test_get_article_extractor: get_extractor("article") returns ArticleExtractor instance
       - test_get_gallery_extractor: get_extractor("gallery") returns GalleryExtractor instance
       - test_unsupported_content_type: get_extractor("podcast") raises UnsupportedContentTypeError, message contains "Supported:"
       - test_stub_raises_not_implemented: each adapter's extract() raises NotImplementedError
       - test_adapters_satisfy_protocol: isinstance(VideoExtractor(), Extractor) is True for all 4

    6. Run `pytest tests/test_router.py -x`
  </action>
  <verify>
    <automated>cd /Users/wendy/work/content-co/content-extractor && pytest tests/test_router.py -x --no-header -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep "runtime_checkable" src/content_extractor/adapters/base.py` exists
    - `grep "class Extractor" src/content_extractor/adapters/base.py` exists
    - `grep "NotImplementedError" src/content_extractor/adapters/video.py` exists
    - `grep "UnsupportedContentTypeError" src/content_extractor/router.py` exists
    - `grep 'register("video"' src/content_extractor/router.py` exists
    - `grep 'register("gallery"' src/content_extractor/router.py` exists
    - `pytest tests/test_router.py -x` passes with 0 failures
  </acceptance_criteria>
  <done>Adapter Protocol defined, 4 stub adapters registered, router dispatches by content_type, unknown types produce clear error. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Atomic output writer with idempotency guard</name>
  <files>
    src/content_extractor/output.py,
    tests/test_output.py
  </files>
  <read_first>
    /Users/wendy/work/content-co/content-extractor/src/content_extractor/models.py,
    /Users/wendy/work/content-co/content-extractor/.planning/phases/01-foundation-data-models/01-RESEARCH.md
  </read_first>
  <behavior>
    - write_json_atomic writes bytes via temp file + rename, file contains valid JSON
    - write_json_atomic cleans up temp file on failure (no .tmp left behind) per QUAL-03
    - write_text_atomic writes UTF-8 text via temp file + rename
    - write_text_atomic cleans up temp file on failure per QUAL-03
    - is_extracted returns True when .extraction_complete marker exists
    - is_extracted returns False when marker does not exist
    - mark_complete creates .extraction_complete marker file
    - clear_marker removes .extraction_complete file (for --force) per FOUND-05
    - write_extraction_output writes transcript.json + analysis.json + structured_text.md + marks complete
    - write_extraction_output skips if already extracted (idempotent) per FOUND-05
    - write_extraction_output writes when force=True even if already extracted per FOUND-05
    - structured_text.md follows D-04 report format: # Title, ## Summary, ## Key Takeaways, ## Full Transcript/Content, ## Analysis
  </behavior>
  <action>
    1. Create `src/content_extractor/output.py`:
       - Import orjson, Path, and models (ExtractionResult, Transcript, AnalysisResult)
       - `MARKER_FILE = ".extraction_complete"`
       - `write_json_atomic(path: Path, data: bytes) -> None`: write to path.with_suffix(path.suffix + ".tmp"), then rename. Cleanup .tmp in except+raise.
       - `write_text_atomic(path: Path, text: str) -> None`: same pattern with write_text(encoding="utf-8").
       - `is_extracted(content_dir: Path) -> bool`: return (content_dir / MARKER_FILE).exists()
       - `mark_complete(content_dir: Path) -> None`: (content_dir / MARKER_FILE).touch()
       - `clear_marker(content_dir: Path) -> None`: (content_dir / MARKER_FILE).unlink(missing_ok=True)
       - `_render_structured_text(result: ExtractionResult, item_title: str, item_author: str, item_platform: str, item_publish_time: str) -> str`:
         Per D-04 format:
         ```
         # {title}

         **Author:** {author} | **Platform:** {platform} | **Published:** {publish_time}
         **Engagement:** {likes} likes, {comments} comments, {shares} shares

         ## Summary

         {placeholder -- populated by analysis phase}

         ## Key Takeaways

         {placeholder -- populated by analysis phase}

         ## Full Transcript/Content

         {result.raw_text}

         ## Analysis

         {placeholder -- populated by analysis phase}
         ```
       - `write_extraction_output(content_dir: Path, result: ExtractionResult, content_item: "ContentItem", *, force: bool = False) -> bool`:
         - If is_extracted(content_dir) and not force: return False (skipped)
         - If force: clear_marker(content_dir)
         - Write transcript.json: `orjson.dumps(result.transcript.model_dump(), option=orjson.OPT_INDENT_2)` if transcript exists, else write minimal JSON `{"content_id": ..., "content_type": ..., "full_text": result.raw_text}`
         - Write analysis.json: placeholder AnalysisResult with content_id/content_type only (populated by Phase 8)
         - Write structured_text.md via _render_structured_text
         - mark_complete(content_dir)
         - Return True (written)

    2. Create `tests/test_output.py`:
       - test_write_json_atomic: write, read back, verify content matches
       - test_write_json_atomic_cleanup_on_failure: mock write to raise IOError, verify no .tmp file left (QUAL-03)
       - test_write_text_atomic: write, read back, verify UTF-8 content
       - test_write_text_atomic_cleanup_on_failure: same pattern (QUAL-03)
       - test_is_extracted_false: empty dir -> False
       - test_is_extracted_true: dir with .extraction_complete -> True
       - test_mark_complete: creates marker file
       - test_clear_marker: removes marker file, no error if missing
       - test_idempotency_skip: write once, second call returns False (FOUND-05)
       - test_idempotency_force: write once, force=True second call returns True (FOUND-05)
       - test_write_extraction_output_files: verify transcript.json, analysis.json, structured_text.md all created
       - test_structured_text_format: verify markdown contains "# " title, "## Summary", "## Key Takeaways", "## Full Transcript/Content", "## Analysis" (D-04)

    3. Run `pytest tests/test_output.py -x`
  </action>
  <verify>
    <automated>cd /Users/wendy/work/content-co/content-extractor && pytest tests/test_output.py -x --no-header -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep "write_json_atomic" src/content_extractor/output.py` exists
    - `grep "write_text_atomic" src/content_extractor/output.py` exists
    - `grep ".extraction_complete" src/content_extractor/output.py` exists
    - `grep "orjson" src/content_extractor/output.py` exists (uses orjson, not stdlib json)
    - `grep "unlink(missing_ok=True)" src/content_extractor/output.py` exists (cleanup)
    - `grep "## Summary" src/content_extractor/output.py` exists (D-04 template)
    - `grep "## Key Takeaways" src/content_extractor/output.py` exists (D-04 template)
    - `pytest tests/test_output.py -x` passes with 0 failures
  </acceptance_criteria>
  <done>Atomic output writer writes transcript.json, analysis.json, and structured_text.md safely. Idempotency via .extraction_complete marker works. --force clears marker and re-writes. No partial files on failure. structured_text.md follows D-04 report format.</done>
</task>

</tasks>

<verification>
All Phase 1 Plan 02 tests pass:
```bash
pytest tests/test_router.py tests/test_output.py -x --no-header -q
```

Combined with Plan 01:
```bash
pytest tests/ -x --no-header -q
```
</verification>

<success_criteria>
- Adapter Protocol defined with runtime_checkable
- 4 stub adapters registered and dispatch correctly
- Unknown content_type raises clear error
- Atomic writes never leave partial files
- Idempotency guard skips already-extracted items
- Force flag re-processes items
- structured_text.md follows D-04 report format
- All tests green
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-data-models/01-02-SUMMARY.md`
</output>
